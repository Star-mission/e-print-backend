/orders/create这个端口要修改
通过订单号索引看有没有已经存在的订单，然后分情况讨论:
1. 如果库里没有就直接创造，版本号为1
2. 如果有，那就查他的订单状态
2.1 如果是被拒绝，那就覆盖，版本号为1——这对应之前拒绝的订单。
2.2 如果订单状态是approved或者inproduction(具体的订单状态我应该是写在order.ts里)，那么就新建版本号+1——这针对已经通过进入生产的订单，但是又发现了要补充的项目。
